# Only needed to run scripts/make_pdf.py.
# WeasyPrint needs system libraries too. On Debian/Ubuntu:
#   sudo apt-get install libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz0b libffi-dev
# On macOS:
#   brew install pango libffi
weasyprint>=60
